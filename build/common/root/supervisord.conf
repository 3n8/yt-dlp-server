[unix_http_server]
file=/config/run/supervisor.sock
chmod=0700

[supervisorctl]
serverurl=unix:///config/run/supervisor.sock

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

[supervisord]
logfile=/config/supervisord.log
logfile_maxbytes=10MB
logfile_backups=5
loglevel=debug
nodaemon=true
pidfile=/config/run/supervisord.pid

[include]
files=/etc/supervisor/conf.d/*.conf
